# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Jeff page
GET     /jeff                           controllers.Application.jeff()

# Pinelake page
GET     /pinelake                   controllers.Application.pinelake()

# Gallery page
GET     /gallery                   controllers.Application.gallery()

# Blog page
GET     /blog                   controllers.Application.blog()

# contact page
GET     /contact                   controllers.Application.contact()

# events page
GET     /events                   controllers.Application.events()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
